name: Playwright Tests

on:
  workflow_dispatch:
    inputs:
      titulo_execucao:
        description: 'Título personalizado para o Webhook'
        required: true
        default: '🚀 Relatório de Testes Automatizados com Playwright'

jobs:
  test:
    name: 'Testes Playwright'
    runs-on: ubuntu-latest
    
    steps:
      - uses: actions/checkout@v4
      
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 18
          cache: 'npm'
      
      - name: Instalar dependências
        run: npm ci
      
      - name: Instalar Playwright Browsers
        run: npx playwright install --with-deps

      - name: Executar testes Playwright
        id: playwright_tests
        run: npx playwright test
        continue-on-error: true
      
      - name: Retry testes com falha
        if: steps.playwright_tests.outcome == 'failure'
        run: npx playwright test --retries=2

      - name: Fazer upload dos logs
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: playwright-logs
          path: ./logs 
          retention-days: 5

      - name: Enviar relatório para Webhook
        if: always()
        env:
          WEBHOOK_URL: ${{ secrets.WEBHOOK_URL }}
        run: |
          node ./libs/Webhook/report-webhook.js ./logs/results/output.xml --titulo "${{ github.event.inputs.titulo_execucao }}"

      - name: Verificar resultado final dos testes
        if: steps.playwright_tests.outcome == 'failure'
        run: |
          echo "Um ou mais testes falharam após todas as retentativas."
          exit 1